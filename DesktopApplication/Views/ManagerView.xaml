<UserControl x:Class="CSWBManagementApplication.Views.ManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CSWBManagementApplication.Views"
             xmlns:views="clr-namespace:CSWBManagementApplication.Views.ManagerViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding ViewName}"
                   FontSize="42"
                   FontWeight="Bold"
                   Foreground="{StaticResource SolidLightGray}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
        />
    </Grid>
</UserControl>
